import { Cart } from 'react-bootstrap-icons';

const CartWidget=() => {
    return(
        <div>
        <Cart size={20} />
        <span>5</span>
        </div>
    );
};

export default CartWidget;